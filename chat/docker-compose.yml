version: '3'
services:
  chatbot:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
    ports:
      - 8080:8080
    volumes:
      - ./.cache:/app/.cache
